# Swaks is an smtp of CURL, install it first:
curl http://www.jetmore.org/john/code/swaks/files/swaks-20130209.0/swaks -o swaks

# Set the permissions for the script so you can run it
chmod +x swaks

# It's based on perl, so install perl
sudo apt-get -y install perl

./swaks --auth \
    --server smtp.mailgun.org \
    --au postmaster@findmyitem.app \
    --ap 1c60b965de6e7dcd7c71726e9a2ce597-324e0bb2-8fc2c952 \
    --from postmaster@findmyitem.app \
    --to <personal_email> \
    --h-Subject: "Hello" \
    --body 'Testing some Mailgun awesomeness!'